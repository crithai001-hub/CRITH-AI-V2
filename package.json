{
  "name": "crith",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Crith — critical-thinking provocations for AI responses on ChatGPT, Claude, and Gemini.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "generate-key": "bash scripts/generate-key.sh"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.47.10"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.34",
    "@types/chrome": "^0.0.287",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
